Classpath resources (src/main/resources)
========================================

This folder is intentionally empty except for this note. Add files here when the
library needs resources on the classpath (for example properties bundles or
templates).

The GPL license text is not duplicated here: the root LICENSE file is copied
into META-INF/LICENSE in the JAR by the Maven build (see pom.xml).
